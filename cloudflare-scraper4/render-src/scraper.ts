import * as cheerio from 'cheerio';
// Temporary: full restore follows in next commit if this fails size limits
export function networkApiProducts() { return []; }
export function numberFromText() { return 0; }
